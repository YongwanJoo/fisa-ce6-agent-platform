apiVersion: v1
kind: Service
metadata:
  name: sre-agent-svc
  namespace: sre-agent
spec:
  selector:
    app: sre-agent
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: LoadBalancer
